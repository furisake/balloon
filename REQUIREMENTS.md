# balloon 要件定義書

## 1. 概要

`balloon`
は、青空文庫形式の文章を「読む」「書く」「確認する」ための日本語Webアプリケーションである。

主要機能は以下の3つとする。

-   **本棚 / Reader**: 青空文庫の作品を検索し、Readerで読む。
-   **Editor / Preview**: 青空文庫記法を扱える原稿EditorとPreview。
-   **ガイド**:
    balloonの使い方、青空文庫記法、参考資料、コントリビュート情報を提供する。

アプリはオープンソースとし、ライセンスは **MIT License**
とする。コントリビュートを歓迎する旨をREADMEおよびガイドに明記する。

UIは日本語固定とし、i18n基盤は導入しない。

------------------------------------------------------------------------

## 2. 基本技術方針

-   Next.js App Router
-   React
-   TypeScript
    -   `strict: true`
    -   `any` は原則禁止
    -   外部ライブラリ等の事情でやむを得ない場合のみ、局所的なESLint
        ignore等で許可
-   Tailwind CSS
-   shadcn/ui
-   MDX: `@next/mdx`
-   Editor: CodeMirror 6
-   テスト: Jest
-   ログ: Pino
-   パッケージ管理: npm
-   フォーマット: Prettier
-   Lint: ESLint
-   デプロイ先としてVercelを想定するが、Vercel固有機能への依存を避け、一般的なNext.js実行環境でも動作すること
-   依存ライブラリのバージョンおよびNode.jsバージョンは本要件では固定しない
-   `package-lock.json` を利用する
-   外部ライブラリは必要最小限とし、標準機能または既存依存で代替できる場合は新規依存を追加しない
-   状態管理はReact Context /
    hooksで足りる限り、Zustand等の状態管理ライブラリを導入しない
-   フォーム管理ライブラリは導入しない
-   バリデーションライブラリは導入せず、必要箇所のみ実装する
-   内部APIのためOpenAPI / tRPC等は導入せず、TypeScript型を共有する

有名かつ十分にメンテナンスされているライブラリであれば、以下の用途では外部ライブラリを利用してよい。

-   CSVパース
-   IndexedDBラッパー
-   ファジー検索（Fuse.jsを第一候補とする）

採用時点で保守状況・利用実績を確認すること。

------------------------------------------------------------------------

## 3. 実装原則

### 3.1 UIに複雑なロジックを持たせない

UIコンポーネントは表示とユーザー操作の橋渡しを主責務とする。

以下のような処理はUIから分離した純粋なTypeScriptモジュール、または適切な
`lib` に置く。

-   青空文庫CSV取得・パース・正規化
-   ファジー検索
-   本文取得
-   ZIP展開
-   文字コード判定・変換
-   Tokenizer
-   Parser
-   Linter
-   差分解析
-   本文内検索
-   IndexedDBの複雑な永続化処理
-   履歴上限管理
-   本文キャッシュLRU管理

Route Handlerも薄くし、可能な限り `lib` の処理を呼び出すだけにする。

### 3.2 設定値と定数

-   `lib` 固有かつ不変な定数は、その `lib` 内に置く。
-   将来変更されうる設定値は `config` に置く。
-   `lib` から `config` を直接importしない。
-   設定値は利用側が `lib` に引数として渡す。
-   GitHub
    URL、青空文庫公式資料URL、Proofreader等、複数箇所から参照する外部URLは定数化する。
-   MDXからも必要に応じてこれらの定数をimportして利用する。

### 3.3 Server / Client Component

Server Componentを基本とする。

以下のようにブラウザAPIまたはインタラクションが必要な部分のみClient
Componentとする。

-   CodeMirror
-   IndexedDB
-   localStorage
-   テーマ切り替え
-   Readerの操作UI
-   Editor / Previewのインタラクション

### 3.4 不明点の扱い

本要件に明記されていない細部については、以下を優先する。

1.  既存の責務分離方針
2.  実装の単純さ
3.  依存の少なさ
4.  ブラウザ / Next.js / CodeMirror等の標準挙動
5.  独自機能を追加しないこと

仕様を独自判断で拡張しない。

------------------------------------------------------------------------

## 4. ホーム

アプリ起動時はランチャー型のホーム画面を表示する。

選択肢は以下の3つ。

-   本棚
-   Editor
-   ガイド

大きなダッシュボード等にはせず、シンプルなランチャーとして構成する。

全画面からテーマを変更可能とする。

------------------------------------------------------------------------

## 5. テーマ

テーマは以下の3種類。

-   Light
-   Dark
-   Sky

初期テーマは **Light**。

テーマ設定はアプリ全体で1つだけ保持し、`localStorage`
に保存して復元する。

### Skyテーマ

-   青空色と白をUI全体の基調とする。
-   Editor / Preview /
    Reader等の本文領域は、文庫本の紙を思わせる薄い生成り・薄茶色とする。
-   可読性を優先する。
-   派手なグラデーション等は避ける。

Reader / Previewの本文フォントは **Noto Serif JP**
のWebフォントを使用する。

Editorは日本語対応の等幅フォントを使用する。

------------------------------------------------------------------------

## 6. 本棚

### 6.1 役割

本棚はReaderの入口であり、読書履歴を表示するページとする。

上部固定ヘッダーに以下を配置する。

-   ホームへ戻る
-   検索窓
-   検索実行ボタン
-   テーマ切り替え

検索窓のプレースホルダーは「作品名・著者名で検索」程度の簡潔な文言とする。

### 6.2 履歴

-   本をReaderで開いた時点で履歴へ追加する。
-   同じ本を再度開いた場合は新規項目を増やさず、最終閲覧日時と読書位置を更新する。
-   最大 **1,000件**。
-   1,001件目が追加された場合は最終閲覧日時が最も古い履歴を削除する。
-   新しい順に表示する。
-   50件ずつ表示する無限スクロール。
-   スマートフォンは1列。
-   PCは画面幅に応じて2〜3列のグリッド。
-   カードに表示するのは以下のみ。
    -   作品名
    -   著者名
-   最終閲覧日時や読書位置はカードには表示しない。
-   カードタップでReaderを開き、保存済み読書位置から自動再開する。
-   各カードに削除ボタンを常時表示する。
-   個別削除は確認なし、Undoなしで即時実行する。

履歴と読書位置は一対一のデータとして扱い、不要に別データへ分割しない。

日時は内部ではUnix timestampで保持する。UI表示が必要な場合は
`Asia/Tokyo` 固定とする。

### 6.3 履歴全削除

本棚リスト上部に「履歴をすべて削除」を配置する。

-   実行前に確認Dialogを表示する。
-   実行すると以下をすべて削除する。
    -   履歴
    -   読書位置
    -   本文キャッシュ

個別履歴削除でも、その履歴に対応する本文キャッシュを削除する。

### 6.4 空状態

履歴0件では、以下のような簡潔な案内のみ表示する。

> まだ読んだ本はありません。上の検索から追加してください。

過剰なEmpty State UIは不要。

------------------------------------------------------------------------

## 7. 青空文庫検索

### 7.1 データソース

青空文庫公式の作品一覧CSVを1つ固定して利用する。

CSVの取得・パース・正規化は独立した `lib`
とする。検索ロジックやUIからCSVの列定義を直接扱わない。

CSVは可能ならサーバー側でキャッシュする。

-   TTL: **1時間**
-   キャッシュが利用可能な環境では利用する。
-   キャッシュがなくても正常に動作すること。

### 7.2 検索

-   検索対象: 作品名・著者名
-   ファジー検索
-   一致度順のみ
-   1回50件
-   無限スクロール
-   自動検索はしない
-   Enterキーまたは検索ボタンで実行
-   検索文字列がある間は本棚履歴表示を検索結果表示へ切り替える
-   検索をクリアすると本棚履歴へ戻る
-   Readerから本棚へ戻った際、以前の検索語・検索結果位置は復元しない
-   0件の場合は「見つかりませんでした」のみ表示
-   取得失敗時は簡潔なエラー + 「再試行」のみ表示

検索結果カードの基本表示:

-   作品名
-   著者名

同一作品に複数の本文ファイル候補がある場合、Dialogで選ばせず、**検索結果上で別々の項目として表示**する。

その場合は識別用に小さく以下も表示する。

-   ファイル名
-   形式 / 文字コード

検索結果から本を開いた時点で履歴に追加する。

------------------------------------------------------------------------

## 8. 本文取得

本文はNext.jsバックエンド経由で取得する。

基本フロー:

1.  クライアントは作品ID / ファイル識別子を指定する。
2.  サーバー側で公式カタログから取得先を解決する。
3.  青空文庫から本文を取得する。
4.  必要ならZIPを展開する。
5.  Shift_JIS等をデコードする。
6.  アプリ内部ではUnicode文字列として扱う。
7.  Readerへ返す。

クライアントから任意の外部URLを渡してサーバーにfetchさせるAPIは作らない。

例:

`/api/fetch?url=https://...`

のような設計は禁止する。

本文取得も可能ならサーバー側でキャッシュする。

-   TTL: **1時間**
-   キャッシュなしでも動作すること

独自User-Agent指定は不要。

明示的な独自タイムアウト要件は設けない。

本文取得・デコード・Parse等に失敗した場合、Readerは本文を部分表示せずエラー状態とし、「再試行」を表示する。

------------------------------------------------------------------------

## 9. ブラウザ側本文キャッシュ

Readerで取得した本文はIndexedDBにキャッシュする。

-   最大 **直近10冊**
-   LRU方式
-   11冊目をキャッシュする場合、最終利用が最も古い本文キャッシュを自動削除する
-   キャッシュが存在する場合は本文を再取得しない
-   容量等の理由でキャッシュできない場合でもReader自体は正常に動作すること
-   キャッシュ状態はユーザーへ表示しない
-   キャッシュ単独削除UIは作らない
-   ユーザー操作によるキャッシュ削除は履歴削除によってのみ行う
-   LRUによる自動削除は例外として許可する

画像は本文キャッシュの対象としない。

------------------------------------------------------------------------

## 10. Reader

### 10.1 本文表示

Reader / Previewは共通の本文表示エンジンを利用する。

共通化対象の概念:

-   AST → 表示
-   縦書き / 横書き
-   文字サイズ
-   1行 / 1列の文字数設定
-   レスポンシブ余白
-   本文表示領域上限
-   画像
-   `sourceRange` 対応
-   検索ハイライト

Reader / Preview固有UIまで無理に共通化しない。

### 10.2 縦書き / 横書き

-   横書き: 上 → 下への連続スクロール
-   縦書き: 右 → 左への連続スクロール
-   ページめくり方式にはしない

本文にはレスポンシブな上下左右の余白を設ける。

横書きはPC大画面で1行が長くなりすぎないよう最大幅を設ける。目安は60〜80ch程度だが、具体値は可読性を優先して実装する。

縦書きも同様に表示領域へ適切な上限を設ける。

### 10.3 表示設定

Readerでは以下を変更可能とする。

-   縦書き / 横書き
-   文字サイズ（4段階）
-   1行 / 1列あたりの文字数
    -   自動
    -   自由入力

Readerの表示設定とPreviewの表示設定は**別々に保存**する。

共通の設定型・共通UIを利用してよいが、保存先を引数等で指定できる構造にする。

例:

-   `reader`
-   `preview`

保存先は `localStorage`。

### 10.4 読書位置

読書位置は元テキストの **source offset** ベースで保存する。

これにより以下が変更されても同じ本文位置へ復帰できることを目指す。

-   縦書き / 横書き
-   文字サイズ
-   文字数設定
-   画面サイズ

Readerを開いた際、保存済み位置があれば自動復帰する。

読書中は位置を随時自動保存する。

### 10.5 Reader操作UI

画面上部・左寄せに、目立たない小さな操作ボタンを常時表示する。

パネルは3種類に分ける。

1.  メイン操作パネル
2.  目次パネル
3.  本文内検索パネル

見出しがない作品では目次ボタン自体を表示しない。

パネル:

-   デスクトップ: サイドパネル
-   スマートフォン: 下から出るSheet

メイン操作パネルには少なくとも以下を含む。

-   本棚へ戻る
-   先頭へ戻る
-   表示設定
-   テーマ変更

### 10.6 目次

青空文庫の見出し注記をParserで解析し、目次を自動生成する。

-   大見出し / 中見出し / 小見出しを階層表示
-   インデントで階層を表現
-   項目タップで対応する `source offset` へジャンプ
-   見出しがない場合は目次UIを出さない
-   現在読んでいる見出しの追従ハイライトは不要
-   目次パネル固有の状態は極力持たず、ASTから生成する簡単な方式とする

### 10.7 本文内検索

本文内検索はReader専用パネルで提供する。

検索ロジックはUIから分離し、将来別アルゴリズムへ差し替えやすくする。

初期実装は最低限とする。

-   部分一致を基本とする
-   必要最低限の文字列正規化を行ってよい
-   高度な検索機能は不要
-   結果件数上限なし
-   50件ずつ無限スクロール
-   一致箇所一覧を表示
-   「前へ / 次へ」は不要
-   各結果に一致文字列と、その後ろを中心とした短いスニペットを表示
-   前側のスニペットは実装が単純なら含めてよい
-   結果項目タップで該当位置へスクロール
-   検索中は一致箇所を本文上ですべてハイライト
-   検索パネルを閉じたら以下をクリア
    -   検索キーワード
    -   検索結果
    -   本文ハイライト

縦書き / 横書きで検索ハイライトのデザインは同じとする。

### 10.8 その他Reader挙動

-   テキスト選択・コピーはブラウザ標準に任せる
-   独自の共有・引用・辞書メニューは不要
-   キーボードスクロールはブラウザ標準に任せる
-   画像タップ拡大は不要
-   印刷専用対応は不要

------------------------------------------------------------------------

## 11. Parser / Tokenizer

### 11.1 基本構成

青空文庫記法の解析は、正規表現の寄せ集めではなく、

`Tokenizer → Parser`

の2段階構成とする。

正規表現をTokenizer内部の局所的判定に利用することは許可する。

Tokenizer / Parser /
LinterはUIから完全に独立した純粋なTypeScriptモジュールとする。

Web
Worker、Jestテスト、将来のCLI等から再利用できることを意識するが、独立npm
packageやmonorepoにはしない。

### 11.2 AST

ASTは段落単位を基本ブロックとする。

段落内部にinline nodeを持つ。

例:

-   plain text
-   ruby
-   emphasis / 傍点
-   注記
-   外字
-   その他対応する青空文庫inline記法

見出し、改ページ等はblock nodeとして扱う。

**すべてのAST nodeに元テキストの `sourceRange { from, to }`
を必須で持たせる。**

`sourceRange` は以下で共通利用する。

-   Editor ↔ Preview位置同期
-   Reader読書位置
-   目次ジャンプ
-   本文内検索
-   Diagnostic
-   ハイライト

未知の青空文庫記法は、可能な限りエラーにせずプレーンテキストとして保持・表示する。

既知記法として認識したにもかかわらず解析不能な壊れた入力は、適切にエラーとして扱う。

------------------------------------------------------------------------

## 12. 差分解析

EditorではParser / Linterをリアルタイムに動作させる。

全文を毎回解析するのではなく、**差分解析**を行う。

基本方針:

-   変更された段落を中心に再解析する
-   未変更部分の解析結果を再利用する
-   前後文脈が必要な記法では必要な範囲まで再解析する
-   最終結果は全文解析結果と一致すること

Parserと差分解析の設計を分離しすぎず、実装コストを抑えながら一貫して扱う。

解析はWeb Workerから呼び出せる構造にする。

Web Worker層自体は薄いadapterとし、複雑なロジックを持たせない。

------------------------------------------------------------------------

## 13. Linter / 校正

青空文庫向けProofreader等を参考に、リアルタイム校正を行う。

-   指摘箇所をEditor上でハイライト
-   指摘理由を表示
-   指摘の種類は最低限色等で区別してよい
-   過剰な色分けはしない
-   指摘件数一覧は不要
-   「無視する」機能は不要
-   即時反映
-   差分解析を利用
-   専用の複雑なPopover / Dialogを増やさない
-   理由や補助情報は既存の簡単なエラー指摘表示内に含める

青空文庫で直接扱えない / Shift_JIS
Exportできない文字もリアルタイムLinterで常時ハイライトする。

対応する外字注記候補が分かる場合は、同じ指摘表示内に候補を併記する。

自動変換専用UIは作らない。

------------------------------------------------------------------------

## 14. Editor

### 14.1 基本

CodeMirror 6を利用する。

Editorの本文領域は日本語対応の等幅フォントを利用する。

以下を有効にする。

-   行折り返し
-   改行単位の行番号
-   現在行ハイライト
-   全角スペース可視化
-   半角スペース可視化
-   タブ可視化
-   改行可視化

括弧対応ハイライトは要件外。

Tabキー、マルチカーソル等は要件として指定せず、CodeMirror標準挙動に任せる。

### 14.2 Editor上部固定ツールバー

Editor上部に固定ツールバーを設ける。

本文をスクロールしても常時表示する。

スマートフォンでは1段の横スクロール式とし、折り返さない。

含めるもの:

-   Editor操作パネルを開くボタン
-   青空文庫記法入力補助
-   Undo
-   Redo
-   検索 / 置換

検索 / 置換はCodeMirror標準UIを利用する。

正規表現検索がCodeMirror標準で利用可能なら、そのまま有効にする。

独自検索 / 置換エンジンは作らない。

### 14.3 青空文庫記法入力補助

よく使う記法は常時見えるツールバーへ配置する。

利用頻度が低いものは簡単な形で隠してよい。

Popover / Dialog等を多用せず、可能な限り単純な入力補助とする。

実装コストを抑えることを優先する。

### 14.4 Editor操作パネル

含めるもの:

-   新規原稿
-   `.txt` 読み込み
-   Export
-   テーマ切り替え
-   ホームへ戻る

表示方法:

-   デスクトップ: サイドパネル
-   スマートフォン: 下から出るSheet

ドラッグ＆ドロップによるファイル読み込みは対応しない。

画像貼り付け / 画像ドラッグ＆ドロップも対応しない。

### 14.5 原稿名

Editor上に現在の原稿名を常時表示し、編集可能とする。

-   新規原稿: `untitled.txt`
-   ファイル読込時: 元ファイル名
-   `.txt` 拡張子は必須ではない
-   Export時に自動で `.txt` を付けない
-   空文字禁止
-   空白のみ禁止
-   OSでファイル名に使用できない文字は入力時点で禁止
-   貼り付けも同じバリデーションを通す
-   原稿名変更も自動保存対象

### 14.6 文字数 / 行数

Editor下部に固定ステータスバーを表示する。

常時表示:

-   保存中 / 保存済み / 保存失敗
-   原稿文字数
-   本文文字数
-   行数

原稿文字数: 青空記法を含む原稿全体の文字数。

本文文字数:
Parserの解析結果を利用し、青空記法や注記等を除いた実際の本文表示を基準とする。

行数: 元テキストの改行単位。

校正指摘件数は表示しない。

------------------------------------------------------------------------

## 15. Editor自動保存

Editorの状態はIndexedDBへ自動保存する。

入力停止後 **500ms debounce**。

以下を同じ共通保存パイプラインで保存する。

-   本文
-   原稿名
-   カーソル位置
-   選択範囲
-   表示位置

新規原稿、ファイル読み込み、通常編集で例外的な保存経路を作らず、可能な限り同じ保存処理へ流す。

-   新規原稿作成時も保存対象
-   `.txt` 読込直後も保存対象
-   Editor起動時、保存済み原稿があれば自動復元
-   保存済み原稿がなければ空の新規原稿を開く
-   元ファイル名 / 編集後ファイル名も復元する
-   保存開始: 「保存中」
-   保存成功: 「保存済み」
-   保存失敗: 「保存失敗」
-   保存失敗時に専用再試行キューは作らず、次の編集時に通常のdebounce保存を再試行する

Undo / RedoはCodeMirror標準機能のみとする。

Undo履歴をIndexedDBへ永続化しない。

------------------------------------------------------------------------

## 16. Import / Exportと文字コード

### 16.1 内部表現

アプリ内部では文字コード付きバイト列を扱い続けず、入力境界でdecodeしてUnicode文字列として扱う。

Editor / Parser / Linter / Preview / Reader /
IndexedDB上の原稿はUnicode文字列を基本とする。

### 16.2 Import

`.txt` 読み込みはUTF-8 / Shift_JISの両方を自動判定する。

どちらでも正常に解釈できなければエラー表示する。

判定した文字コード名はユーザーへ表示しない。

### 16.3 Export

Shift_JIS Exportを提供する。

-   Unicode文字列 → Shift_JIS
-   改行: CRLF
-   Shift_JISへ変換できない文字が1つでも残っている場合、Exportを中止
-   変換不能文字は全件まとめて検出
-   通常Linterと同じDiagnostic / ハイライト / 理由表示UIを利用
-   変換不能文字はExport操作時だけでなく、通常編集時から常時ハイライト
-   Export成功時のToast / 成功メッセージは不要
-   ファイル保存開始をもって成功扱い
-   Export失敗時のみEditor内へインライン表示

文字コード変換、変換不能文字判定、外字注記候補マッピングには必ずテストを書く。

------------------------------------------------------------------------

## 17. Preview

### 17.1 レイアウト

デスクトップ:

-   Editor / Previewを初期50:50で左右分割
-   境界をドラッグして比率変更可能
-   変更した分割比率は保存しない
-   Editorを開くたび50:50

スマートフォン:

-   Editor / Previewを切り替えるUI
-   切り替え時に現在位置を維持する

### 17.2 位置同期

Editor ↔ Previewは**常時双方向同期**する。

-   Editorスクロール → Preview追従
-   Previewスクロール → Editor追従
-   スマートフォンでの切り替え時も対応位置を維持

ピクセル位置を直接同期せず、ASTの `sourceRange` / source
offsetを基準にする。

### 17.3 Preview表示設定

Previewにも表示設定を持たせる。

設定UIはボタン1つに畳む。

含めるもの:

-   縦書き / 横書き
-   文字サイズ
-   文字数設定

Readerと設定UI・設定型は共通化してよいが、設定値自体はReaderと分離する。

PreviewにはReader専用の以下は不要。

-   目次パネル
-   本文内検索パネル

------------------------------------------------------------------------

## 18. 画像

対応するのは**青空文庫記法としての画像**のみ。

対応しないもの:

-   ローカル画像のアップロード
-   画像貼り付け
-   ドラッグ＆ドロップ
-   ローカル `.txt` と同一フォルダの相対画像を独自に解決する仕組み

Reader / Previewでは青空文庫画像注記を解析・レンダリングする。

画像サイズ:

-   青空記法の指定を優先
-   表示領域を超える場合のみアスペクト比を維持してレスポンシブ縮小

取得失敗時:

-   Reader全体をエラーにしない
-   画像部分に代替テキストを表示
-   青空記法から説明文を取得できる場合はそれを優先
-   説明文がなければ「画像の取得に失敗しました」等の固定文言

画像URL等の外部リソースは**許可ドメインリスト**で検証する。

許可ドメインリストは変更されうる設定として `config`
に置き、利用側からRenderer等へ渡す。

許可されていないドメインのリソースは読み込まない。

------------------------------------------------------------------------

## 19. セキュリティ

青空文庫本文、CSV、注記等の外部入力を信頼しない。

**原文由来のHTMLをそのまま `dangerouslySetInnerHTML`
に渡す実装は禁止する。**

Parserで構造化したAST等から、安全なReact要素としてレンダリングする。

外部URLは必要に応じて検証する。

CSV / 本文のサーバー取得先はサーバー側の公式データ /
設定から決定し、クライアントから任意URLを指定させない。

初期版ではAPIレート制限は実装しない。

------------------------------------------------------------------------

## 20. ガイド

ガイドはMDXで作成する。

`@next/mdx` を利用する。

1ページ完結ではなく複数ページへ分割する。

例:

-   `/guide`
-   `/guide/editor`
-   `/guide/reader`
-   `/guide/notation`
-   `/guide/references`
-   `/guide/contributing`

具体的なページ分割は、内容を過剰に細分化しない範囲で調整してよい。

### ガイドUI

-   デスクトップ: サイドバーnavigation
-   スマートフォン: 折りたたみ式navigation
-   ホームへ戻るボタンを常時表示
-   テーマ変更可能
-   GitHubリポジトリへのリンクを常時見える位置に配置
-   ガイド内検索は不要
-   「前へ / 次へ」リンクは不要

### ガイド内容

概要レベルで以下を含める。

-   balloonの概要
-   Readerの使い方
-   Editor / Previewの使い方
-   青空文庫記法
-   リファレンス
-   参考資料
-   青空文庫公式資料への出典
-   Proofreader等の参考実装 / 参考資料
-   オープンソースであること
-   MIT License
-   コントリビュート歓迎

外部参考資料リンクは別タブで開く。

コード例は追加のシンタックスハイライトライブラリを導入せず、MDXの標準的なコードブロック +
Tailwind程度に留める。

------------------------------------------------------------------------

## 21. README / CONTRIBUTING

### README.md

少なくとも以下を記載する。

-   balloonの概要
-   主な機能
-   開発環境の起動方法
-   テスト方法
-   lint / build方法
-   青空文庫公式データ / 仕様等の参照元
-   オープンソースであること
-   MIT License
-   コントリビュート歓迎
-   `CONTRIBUTING.md` への導線

### CONTRIBUTING.md

少なくとも以下を記載する。

-   開発参加方法
-   セットアップ
-   コーディング方針
-   PR方針
-   Parser / Linter等のテスト要件
-   `lint / test / build` の実行方法

------------------------------------------------------------------------

## 22. ロゴ / favicon

アプリ名は **balloon**。

初期版では文字ロゴは不要。

ロゴは以下の方針。

-   赤い風船をモチーフとする
-   青空との組み合わせを意識する
-   SVG
-   faviconにも同じシンボルを流用

------------------------------------------------------------------------

## 23. SEO

基本的なSEOメタデータを用意する。

-   title
-   description
-   OGP

ただし開発初期は検索エンジンへの掲載を避ける。

dev段階では `noindex, nofollow` を設定する。

公開準備が整った際に容易に切り替えられる構造にする。

------------------------------------------------------------------------

## 24. エラー / フィードバックUI

ユーザー向けエラーは簡潔な日本語とする。

技術的詳細はPinoログへ出す。

Pinoの基本ログレベル:

-   `info`
-   `warn`
-   `error`

ブラウザ側ログは必要最小限。

### Toast禁止

**Toastは使用しない。**

成功・失敗・警告は、該当する画面、パネル、ステータスバー等へインライン表示する。

例外として、ユーザー確認が必要な操作ではshadcn/uiのDialog等を利用してよい。

例:

-   履歴をすべて削除

### ローディング

shadcn/ui等で利用可能なSpinner相当の最低限のローディング表示を行う。

過剰なSkeletonやアニメーションは不要。

------------------------------------------------------------------------

## 25. アクセシビリティ

最低限対応する。

-   キーボード操作
-   適切なARIA
-   フォーカス表示
-   十分なコントラスト

過剰な独自アクセシビリティ機構は不要。

------------------------------------------------------------------------

## 26. テスト

テストフレームワークは **Jest**。

UIコンポーネントの自動テストは必須としない。

Web Worker通信adapter自体のテストも必須としない。

Route Handler自体のテストも必須としない。

重点的にテストする対象:

-   Tokenizer
-   Parser
-   Linter
-   差分解析
-   文字コード判定
-   UTF-8 / Shift_JIS変換
-   Shift_JIS変換不能文字判定
-   外字注記候補マッピング
-   CSV解析
-   CSV正規化
-   本文処理
-   ZIP展開等の純粋ロジック

### Fixture

実際の青空文庫作品本文を大量にfixtureへ含めず、**自作の短いfixture**を利用する。

Parser:

-   対応する青空文庫記法ごとに最低1つのfixtureテストを必須とする。
-   必要な記法では壊れた / 不完全な入力もテストする。

Linter:

-   Proofreader等を参考に実装する各チェック項目について最低1つのfixtureテストを必須とする。
-   必要に応じて正常ケースと指摘ケースを用意する。

差分解析:

少なくとも以下をテストする。

-   1段落だけ変更した場合、変更箇所を中心に再解析される
-   未変更段落の解析結果を再利用できる
-   前後文脈が必要な場合に必要範囲だけ再解析される
-   複数段落変更でも最終結果が全文解析と一致する

カバレッジ率の数値目標は設定しない。

数値上のcoverageより、指定fixture / ケースの網羅を優先する。

------------------------------------------------------------------------

## 27. 推奨実装順序

以下は強制的なコミット順ではないが、依存関係上の推奨実装順序とする。

### Phase 1: 解析コア

-   型
-   Tokenizer
-   Parser
-   Linter
-   sourceRange
-   差分解析
-   fixtureテスト
-   文字コード関連の純粋ロジック

### Phase 2: 共通表示基盤

-   AST renderer
-   Reader / Preview共通本文表示
-   縦書き / 横書き
-   文字サイズ
-   文字数設定
-   余白
-   本文幅
-   画像
-   source offsetによる位置指定

### Phase 3: Editor

-   CodeMirror
-   不可視文字表示
-   シンタックス / Diagnostic表示
-   Web Worker連携
-   入力補助
-   Preview
-   双方向位置同期
-   自動保存
-   Import / Export

### Phase 4: Reader

-   表示設定
-   読書位置保存 / 復元
-   目次
-   本文内検索
-   Reader操作パネル

### Phase 5: 本棚 / API

-   青空文庫CSV
-   ファジー検索
-   本文取得
-   サーバーキャッシュ
-   履歴
-   IndexedDB本文キャッシュ
-   LRU

### Phase 6: ガイド / テーマ / 仕上げ

-   Light / Dark / Sky
-   MDXガイド
-   README
-   CONTRIBUTING
-   MIT License
-   ロゴ / favicon
-   SEO / noindex
-   最低限のアクセシビリティ
-   最終lint / test / build

Codexは途中で人間の確認を要求せず、要件に従って完成まで実装する前提とする。

------------------------------------------------------------------------

## 28. 非目標 / Out of Scope

初期版では以下を実装しない。

-   お気に入り
-   履歴内検索
-   キャッシュ単独削除UI
-   キャッシュ状態表示
-   Readerの独自共有 / 引用 / 辞書メニュー
-   Readerの画像拡大 / Lightbox
-   印刷専用UI / CSS
-   PWA対応
-   i18n
-   CI / GitHub Actions
-   UIコンポーネントの必須自動テスト
-   Web Worker adapterの必須テスト
-   Route Handlerの必須テスト
-   OpenAPI
-   tRPC
-   独立した状態管理ライブラリ
-   フォーム管理ライブラリ
-   汎用バリデーションライブラリ
-   画像アップロード
-   画像貼り付け
-   画像ドラッグ＆ドロップ
-   `.txt` ドラッグ＆ドロップImport
-   ローカルファイルシステム上の画像相対パス解決
-   Reader目次の現在位置追従ハイライト
-   Reader本文検索の「前へ / 次へ」
-   ガイド内検索
-   ガイドの「前へ / 次へ」
-   独自シンタックスハイライトライブラリ
-   Toast
-   APIレート制限（初期版）
-   独自User-Agent
-   独自の通信タイムアウト要件
-   Parser / Linterの独立npm package化
-   monorepo化
-   Undo / Redo履歴の永続化
-   Editor括弧対応ハイライト
-   マルチカーソル等のCodeMirror標準機能に対する独自仕様
-   検索結果 / 検索位置のReaderからの復元
-   Readerの壊れた本文のフォールバック表示

------------------------------------------------------------------------

## 29. 完了条件（Definition of Done）

以下がすべて成功すること。

``` bash
npm run lint
npm test
npm run build
```

これを初期版のDefinition of Doneとする。
